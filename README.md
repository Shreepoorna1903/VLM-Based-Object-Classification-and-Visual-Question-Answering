# VLM-Based-Object-Classification-and-Visual-Question-Answering
Build a computer vision project that fine-tunes a Vision Language Model (Qwen2-VL) on a COCO subset, compares it against a DETR object detection baseline, and evaluates both using standard metrics (mAP50, precision, recall, latency).
